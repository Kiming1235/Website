# Website

This repository contains an Astro + React + Tailwind front-end project that has been stripped of any vendor-specific integrations.

Current production editing/operations are handled in WordPress (Cafe24 hosting).

## Commands

- `npm run dev` – local development
- `npm run build` – production build
- `npm run preview` – preview the production build

